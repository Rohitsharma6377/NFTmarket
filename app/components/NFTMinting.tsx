'use client';

import React, { useState } from 'react';
import { useWeb3 } from '@/context/Web3Context';
import { toast } from 'react-hot-toast';

const NFTMinting = () => {
  // ... rest of your NFTMinting component code
};

export default NFTMinting; 